import React from 'react';
import { Link } from 'react-router';

const Deck = React.createClass({
  render() {
    return (
      <div className='decks'>
        I am a Deck.
      </div>
    )
  }
});

export default Deck;
